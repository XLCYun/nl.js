# NL.js

NL.js 将自然语言片段嵌入程序，用程序上下文表达片段的含义。这里记录讨论代码生成与语义判断时使用的领域术语。

## Language

**自然语言片段**：
嵌入程序、描述一段意图的自然语言内容。片段可能描述一个动作，也可能提出一个判断。
_Avoid_：把所有片段都称作判断条件

**代码生成**：
根据自然语言片段及其上下文构造可执行程序的能力。生成的程序可以计算、执行动作或返回结果。
_Avoid_：纯判断

**纯判断**：
根据已有事实判断一个命题是否成立的能力。询问真人、获取新证据或执行动作不属于判断本身。
_Avoid_：代码生成、执行动作

**判断状态**：
供一次纯判断使用的事实与上下文。判断状态描述当次可观察到的信息。
_Avoid_：判断结论

**判定概率**：
模型对一个命题成立的概率估计。它与最终供程序分支使用的布尔结论是两个概念。
_Avoid_：布尔结论、程度评分
